/** Chapters emerge from the story as it happens — never predefined in advance (§65). */
export interface Chapter {
  id: string;
  number: number;
  title: string;
  summary: string;
  startDate: string;
}
