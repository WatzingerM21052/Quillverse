# Experimental refinement archive

This folder mirrors the numbered active-review groups while separating files
that must not be treated as current candidates:

- `not-preferred/`: technically usable output rejected in visual review;
- `technical-failures/`: opaque-checkerboard or otherwise invalid generation.

Nothing is deleted. These files remain available as visual evidence and prompt
history, but they must not be promoted or integrated directly.
